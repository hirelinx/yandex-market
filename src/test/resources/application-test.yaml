spring:
  r2dbc:
    url: r2dbc:h2:mem:///market-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    username: sa
    password:
  liquibase:
    url: jdbc:h2:mem:market-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    user: sa
    password:
    change-log: classpath:db/changelog/db.changelog-master.yaml

app:
  uploadsDir: ${java.io.tmpdir}/market-test-uploads/
