spring:
  r2dbc:
    url: r2dbc:h2:file:///${java.io.tmpdir}/market;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    username: sa
    password:

  liquibase:
    url: jdbc:h2:file:${java.io.tmpdir}/market;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    user: sa
    password:
    change-log: classpath:db/changelog/db.changelog-master.yaml
