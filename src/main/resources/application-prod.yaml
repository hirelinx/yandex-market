spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  liquibase:
    url: ${LDB_URL}
    user: ${DB_USER}
    password: ${DB_PASS}
    change-log: classpath:db/changelog/db.changelog-master.yaml